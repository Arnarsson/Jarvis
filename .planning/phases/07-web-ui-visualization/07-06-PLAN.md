---
phase: 07-web-ui-visualization
plan: 06
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05]
autonomous: false
---

<objective>
End-to-end verification of web UI and Docker deployment.

This is a checkpoint plan requiring manual verification of all UI features.
</objective>

<context>
@.planning/ROADMAP.md
@server/docker-compose.yml
</context>

<tasks>

<task type="auto">
<name>Task 1: Docker build with web UI</name>
<files>server/Dockerfile</files>
<action>
Ensure Dockerfile includes web templates and static files.
Rebuild: docker compose build --no-cache
</action>
<done>Docker build successful</done>
</task>

<task type="auto">
<name>Task 2: Deploy and verify server starts</name>
<action>
docker compose up -d
docker compose logs -f jarvis-server (verify no errors)
</action>
<done>Server running</done>
</task>

<task type="checkpoint:human-verify">
<name>Task 3: Verify dashboard</name>
<action>
Open http://localhost:8000/ in browser.
Verify:
- Dashboard loads
- Stats cards show data
- Upcoming meetings appear
- Recent captures show
</action>
<resume_signal>Type "approved" when dashboard works</resume_signal>
</task>

<task type="checkpoint:human-verify">
<name>Task 4: Verify timeline</name>
<action>
Navigate to /timeline.
Verify:
- Date picker shows available dates
- Captures load for selected date
- Modal opens on click
- Navigation works
</action>
<resume_signal>Type "approved" when timeline works</resume_signal>
</task>

<task type="checkpoint:human-verify">
<name>Task 5: Verify search</name>
<action>
Navigate to /search.
Verify:
- Search returns results
- Filters work
- Results clickable
</action>
<resume_signal>Type "approved" when search works</resume_signal>
</task>

<task type="checkpoint:human-verify">
<name>Task 6: Verify calendar</name>
<action>
Navigate to /calendar.
Verify:
- Calendar shows events
- Can click events
- Meeting briefs display
</action>
<resume_signal>Type "approved" when calendar works</resume_signal>
</task>

<task type="checkpoint:human-verify">
<name>Task 7: Verify settings</name>
<action>
Navigate to /settings.
Verify:
- System status shows all services
- Integration status accurate
- Sync button works
</action>
<resume_signal>Type "approved" when settings works</resume_signal>
</task>

<task type="auto">
<name>Task 8: Update ROADMAP and STATE</name>
<action>Mark Phase 7 complete in ROADMAP.md and STATE.md</action>
<done>Documentation updated</done>
</task>

</tasks>

<verification>
All manual verifications passed.
</verification>

<success_criteria>
- All pages load without errors
- Dashboard shows live data
- Timeline browsing works
- Search returns results
- Calendar shows events
- Settings shows system status
</success_criteria>

<commit_guidance>
docs(07-06): complete Phase 7 web UI verification
</commit_guidance>
