---
phase: 06-workflow-automation
plan: 07
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06]
autonomous: false
---

<objective>
End-to-end verification of Phase 6 Workflow Automation.

This is a checkpoint plan requiring manual verification.
</objective>

<context>
@.planning/ROADMAP.md
@server/docker-compose.yml
</context>

<tasks>

<task type="auto">
<name>Task 1: Docker rebuild</name>
<action>
docker compose build --no-cache
docker compose up -d
docker compose logs -f jarvis-server (verify no errors)
</action>
<done>Server running with workflow modules</done>
</task>

<task type="checkpoint:human-verify">
<name>Task 2: Verify pattern detection</name>
<action>
Let agent capture for 30+ minutes with repetitive activity.
Check:
- GET /api/workflow/patterns shows detected patterns
- Patterns have frequency counts
</action>
<resume_signal>Type "approved" when patterns detected</resume_signal>
</task>

<task type="checkpoint:human-verify">
<name>Task 3: Verify suggestions</name>
<action>
Navigate to /workflows in browser.
Verify:
- Suggestions tab shows proposals
- Each suggestion has approve/reject buttons
- Clicking approve moves to Active tab
</action>
<resume_signal>Type "approved" when suggestions work</resume_signal>
</task>

<task type="checkpoint:human-verify">
<name>Task 4: Verify execution</name>
<action>
Approve a simple automation (e.g., notification).
Trigger it manually or wait for pattern match.
Verify:
- Automation executes
- Execution appears in history
- Feedback buttons work
</action>
<resume_signal>Type "approved" when execution works</resume_signal>
</task>

<task type="checkpoint:human-verify">
<name>Task 5: Verify safety guardrails</name>
<action>
Try to create/approve a destructive automation.
Verify:
- Destructive actions require manual approval each time
- Cannot set destructive patterns to "auto" tier
</action>
<resume_signal>Type "approved" when guardrails work</resume_signal>
</task>

<task type="checkpoint:human-verify">
<name>Task 6: Verify undo</name>
<action>
After an automation executes:
- Click undo within 24 hours
- Verify action is reversed
</action>
<resume_signal>Type "approved" when undo works</resume_signal>
</task>

<task type="auto">
<name>Task 7: Update ROADMAP</name>
<action>Mark Phase 6 complete in ROADMAP.md</action>
<done>Documentation updated</done>
</task>

</tasks>

<verification>
All manual verifications passed.
</verification>

<success_criteria>
- Patterns detected from screen activity
- Suggestions generated and manageable
- Automations execute correctly
- Safety guardrails prevent destructive auto-execution
- Undo works within time window
</success_criteria>

<commit_guidance>
docs(06-07): complete Phase 6 workflow automation verification
</commit_guidance>
