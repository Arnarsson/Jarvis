# Jarvis Server Environment Configuration Example
# Copy this to .env and fill in your values

# ============================================
# Database (PostgreSQL) - for Docker
# ============================================
POSTGRES_USER=jarvis
POSTGRES_PASSWORD=changeme
POSTGRES_DB=jarvis

# ============================================
# Server Configuration
# ============================================
JARVIS_DATABASE_URL=postgresql+asyncpg://jarvis:changeme@localhost:5432/jarvis
JARVIS_QDRANT_HOST=localhost
JARVIS_QDRANT_PORT=6333
JARVIS_REDIS_HOST=localhost
JARVIS_REDIS_PORT=6379
JARVIS_LOG_LEVEL=INFO

# Data directory (for local development)
# JARVIS_DATA_DIR=/path/to/data
# JARVIS_STORAGE_PATH=/path/to/data/captures

# ============================================
# API Keys
# ============================================
# JARVIS_ANTHROPIC_API_KEY=sk-ant-...

# ============================================
# Google Calendar (Phase 4)
# ============================================
# Place credentials.json from Google Cloud Console at:
# $JARVIS_DATA_DIR/calendar/credentials.json
