# Jarvis Server Environment Configuration
# Copy to .env and customize values

# ============================================
# Database (PostgreSQL)
# ============================================
POSTGRES_USER=jarvis
POSTGRES_PASSWORD=changeme
POSTGRES_DB=jarvis

# ============================================
# Server Configuration
# ============================================
# Full database URL (constructed automatically in docker-compose.yml)
# Only set this if running outside Docker or need custom connection
# JARVIS_DATABASE_URL=postgresql+asyncpg://jarvis:changeme@postgres:5432/jarvis

# Qdrant vector database (defaults work with docker-compose)
JARVIS_QDRANT_HOST=qdrant
JARVIS_QDRANT_PORT=6333

# Storage path for capture files
JARVIS_STORAGE_PATH=/data/captures

# Logging level: DEBUG, INFO, WARNING, ERROR
JARVIS_LOG_LEVEL=INFO

# ============================================
# IMPORTANT: Security Notes
# ============================================
# 1. Change POSTGRES_PASSWORD before production deployment
# 2. All services bind to 127.0.0.1 only (localhost)
# 3. Access the server via Tailscale IP, not public internet
# 4. Never expose ports directly to the internet
